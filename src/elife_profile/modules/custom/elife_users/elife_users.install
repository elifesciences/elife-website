<?php

/**
 * Implements hook_install().
 */
function elife_users_install() {
  features_revert(['elife_users' => ['variable']]);

  // Give user 1 the Administrator role.
  db_insert('users_roles')
    ->fields(['uid' => 1, 'rid' => variable_get('user_admin_role')])
    ->execute();
}
